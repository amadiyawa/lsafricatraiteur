package com.amadiyawa.feature_base.presentation.compose.composable

import androidx.compose.ui.graphics.Color

class AppColor {
    companion object {
        val LightSuccess = Color(0xFF4CAF50) // Green
        val LightOnSuccess = Color(0xFFFFFFFF) // White
        val DarkSuccess = Color(0xFF81C784) // Light Green
        val DarkOnSuccess = Color(0xFF000000) // Black
        val DodgerBlue = Color(0xFF2196F3) // Dodger Blue
        val Coral = Color(0xFFFF5722) // Coral
        val DimGray = Color(0xFF808080) // Dim Gray
        val Charcoal = Color(0xFF2f3135) // Charcoal
        val MintCream = Color(0xFF98FB98) // Mint Cream
        val Tomato = Color(0xFFFF6961) // Tomato
        val SkyBlue = Color(0xFF87CEFA) // Sky Blue
        val DeepSkyBlue = Color(0xFF00BFFF) // Deep Sky Blue
    }
}